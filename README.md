# LunarGate Homebrew Tap

This repository is the official Homebrew tap for LunarGate.

## Install

```bash
brew tap lunargate-ai/tap
brew install lunargate

lunargate --help
```

## Notes

- The formula is maintained automatically by GoReleaser from the main gateway repository.
- If you maintain this repo manually, formulas live in `Formula/`.
